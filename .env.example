# Copy this to apps/web/.env.local and fill in your values.
# Generate secrets with: openssl rand -base64 32
# Generate SMTP key with: openssl rand -hex 32

DATABASE_URL="postgresql://user:pass@host/rabbitmailer?pgbouncer=true&connection_limit=1"
DATABASE_URL_DIRECT="postgresql://user:pass@host/rabbitmailer"

SESSION_SECRET=""
REFRESH_TOKEN_SECRET=""

SMTP_ENCRYPTION_KEY=""

REVALIDATION_SECRET=""

EDGE_CONFIG=""
FALLBACK_TENANT_ID="tenant_dev"

NEXT_PUBLIC_APP_URL="http://localhost:3000"
